<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Front-End</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #007BFF;
            color: #fff;
            padding: 10px;
            text-align: center;
            font-size: 24px;
        }
        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            padding: 20px;
        }
        .block {
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
            padding: 20px;
            margin: 10px;
            width: calc(25% - 20px);
            transition: transform 0.2s;
        }
        .block:hover {
            transform: scale(1.05); /* Increase size on hover */
            cursor: pointer;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2); /* Add a shadow on hover */
        }
        h2 {
            font-size: 18px;
            margin-bottom: 10px;
        }
        .apply-button {
            background-color: #007BFF;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <!--<h1 id="username"></h1>-->
    <header>
        Hope
    </header>
    <div class="container">
        <div class="block">
            <h2>Personal Details</h2>
            <!-- Include personal details form fields here -->
        </div>
        <div class="block">
            <h2>Account Details</h2>
            <!-- Include account details form fields here -->
        </div>
        <div class="block" id="loan-request-block">
            <form action="">
            <h2>Loan Request</h2>
             <button type="button" class="apply-button" onclick="window.location.href='/apply'">Apply</button>
            </form>
        </div>
        <div class="block">
            <form action="">
            <h2>Loan status and repayment</h2>
             <button type="button" class="apply-button" onclick="apply()">Check</button>
            </form>
        </div>
    </div>
    <script>

        let u = window.location.href.split("?")[1].split("=")[1]; //u = username
        // document.getElementById("username").innerHTML=`Welcome ${u}`
        localStorage.setItem("username", u)
        window.location.href = "/customer"
        function apply(){
            window.location.href=`/status/${localStorage.getItem("username")}`
        }
    </script>
    <!-- JavaScript to handle the click event and redirect -->
</body>
</html>